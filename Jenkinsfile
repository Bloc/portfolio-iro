pipeline {
    agent any

    stages {
        stage('Testing') {
            steps {
                sh 'pwd && ' 
            }
        }
    stage('Building') {
            steps {
                sh 'bundle exec jekyll build' 
            }
        }
    }
}
